# 📰 News Bot — умный Telegram-бот для автоматической публикации IT-новостей

![Tests](https://github.com/Golden-boy-jk/News_bot/actions/workflows/ci.yml/badge.svg)
![Coverage](https://img.shields.io/badge/coverage-100%25-brightgreen)
![Python](https://img.shields.io/badge/python-3.11-blue)
![License](https://img.shields.io/badge/license-MIT-green)

News Bot — это полностью автоматизированный агрегатор IT-новостей, который:

- каждый день в **09:00 (Europe/Moscow)** собирает публикации с топ-источников;
- извлекает текст, очищает его и нормализует;
- оценивает важность статьи через **TF-IDF + keyword scoring**;
- сохраняет данные в SQLite;
- публикует лучшие новости дня в Telegram;
- по субботам формирует подборку тулзов 🔧;
- по воскресеньям создаёт недельный дайджест 📰;
- выполняет **мониторинг свежести новостей** и шлёт алерты, если что-то пошло не так.

Проект покрыт **100% тестов**, имеет продакшен-Docker окружение, healthcheck, логирование в файлы и полностью готов к развёртыванию на VPS.

---

# 🚀 Возможности

### ✔ Сбор новостей
- OpenAI  
- Google AI Blog  
- Anthropic  
- HuggingFace  
- Databricks  
- Confluent  
- AWS BigData Blog  
- Docker Blog  
- Python.org / RealPython / PythonWeekly / PyCharm Blog  
- TheHackerNews, GBHackers, CybersecurityNews и др.

### ✔ Парсинг и обработка текстов
- BeautifulSoup + очистка HTML
- удаление `script`, `style`, `noscript`
- автодобавление `<title>` в начало текста
- нормализация и очистка пробельных строк

### ✔ Ранжирование
- TF-IDF по целому корпусу статей
- веса ключевых слов (OpenAI, GPT, Python, Spark, Kafka и т.д.)
- fallback по норме TF-IDF, если ключевых слов нет

### ✔ Хранилище (SQLite)
Таблица `news`:

```text
 
id | url | title | summary | content | source | score | fetched_at

url — уникальный адрес статьи

title — заголовок

summary — краткое описание (2–3 строки)

content — очищенный текст

source — распознанный источник (openai, databricks, docker_blog, …)

score — важность новости

fetched_at — время сохранения (UTC, ISO)

✔ Публикация в Telegram

формат поста: 💡 Что произошло / 📌 Почему это важно / 🔗 Источник / 😅 Юмор + хэштеги

аккуратная нарезка текста: не выходим за лимит 4096 символов

Markdown-разметка

отдельный канал/чат для error-алертов

✔ Планировщик APScheduler

09:00 — ежедневный запуск NewsProfessor.run_for_today()

10:00 — NewsProfessor.run_monitoring() (мониторинг свежести новостей)

✔ Мониторинг свежести

Метод run_monitoring():

проверяет, есть ли вообще новости в БД;

смотрит дату последней новости;

если новостей давно не было — шлёт алерт;

корректно обрабатывает NULL и некорректные значения fetched_at.

✔ Логирование

Модуль logging_utils.py:

stdout-логирование (удобно для Docker);

файловые логи /var/log/news_bot/app.log и /var/log/news_bot/error.log;

ротация логов через RotatingFileHandler;

алерты об ошибках через отдельный Telegram-чат.

✔ HTTP-retry

_fetch_html_with_retry() в link_extractor.py;

_download_with_retry() в text_parser.py;

до 3 попыток, ожидание между ретраями, в случае провала — RuntimeError (закрыто тестами).

✔ Healthcheck

Модуль app.healthcheck:

проверка ENV (TELEGRAM_* и DATABASE_PATH);

проверка доступности БД (SELECT 1);

код возврата 0 / 1 — удобно для Docker HEALTHCHECK.

📅 Контент-план
День	Тема
Пн	🤖 AI и нейросети
Вт	🐍 Python
Ср	📊 Data Engineering
Чт	🔒 Security
Пт	🛠 DevTools и сервисы
Сб	Подборка тулзов
Вс	Недельный дайджест

Формат всех новостей и дайджестов — единый:
💡 Что произошло / 📌 Почему это важно / 🔗 Источник / 😅 Юмор + 2 слоя хэштегов.

---

# 📂 Структура проекта
```app/
│── config.py            # Загрузка настроек из .env
│── db.py                # SQLite + миграции
│── error_notifier.py    # Telegram-алерты об ошибках
│── filters.py           # Фильтрация ссылок по подстроке
│── healthcheck.py       # Проверка окружения и БД
│── link_extractor.py    # HTML → список ссылок (с retry)
│── logging_utils.py     # Логирование + ротация файлов
│── main.py              # Одноразовый запуск бота
│── news_professor.py    # Оркестратор: сбор, парсинг, ранжирование, публикация, мониторинг
│── scheduler_main.py    # APScheduler (09:00 новости, 10:00 мониторинг)
│── scoring.py           # TF-IDF + keyword scoring
│── telegram_bot.py      # Форматирование и отправка сообщений
│── text_parser.py       # HTML → очищенный текст (с retry)
│── text_utils.py        # Утилиты для работы с текстом (лимит Telegram)
tests/
│── test_*.py            # Полный набор тестов (100% coverage)
docker-compose.yml       # Dev окружение
docker-compose.production.yml  # Продакшен-окружение
Dockerfile               # Многоразовый образ бота
README.md
PRODUCTION_DEPLOY.md

```

## 📦 Установка

```bash
git clone https://github.com/Golden-boy-jk/News_bot.git
cd News_bot
pip install -r requirements.txt
```

🧪 Тесты

Проект покрыт 100% тестов.

```Запуск: pytest```

Покрытие:

```pytest --cov=app --cov-report=term-missing:skip-covered```

🛠 Запуск локально (без Docker)

```pip install -r requirements.txt```

```echo > news.db```

```python -m app.scheduler_main```


Планировщик будет ждать 09:00 по Europe/Moscow.

🐳 Docker (Dev)

``docker compose up --build
``

Использует .env и локальную news.db.


🩺 Healthcheck

Контейнер имеет встроенный self-check:
- ``` test: ["CMD", "python", "-m", "app.healthcheck"] ```

Проверяется:

- корректность .env
- доступность БД
- возможность чтения/записи
- отсутствие критических ошибок
- 
```Статус контейнера должен быть: healthy.```

🧱 Docker (Production)

```mkdir -p data logs```
```touch data/news.db```

Создать .env.prod:
```TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...
TELEGRAM_ERROR_CHAT_ID=...
DATABASE_PATH=/app/news.db
```
Запуск продакшена

```docker compose -f docker-compose.production.yml up -d --build```

Проверка:

```docker ps```

```docker logs news-bot --tail 50```

Ожидаемый лог:

Планировщик запущен. Ожидаю ежедневный запуск в 09:00 (Europe/Moscow).

Логи

На хосте:

```logs/app.log``` 

```logs/error.log```

🔧 Архитектура запуска (кратко)

1. Scheduler запускается в 09:00.
2. Выбирает набор сайтов по дню недели.

3. Собирает ссылки.

4. Парсит HTML → текст.

5. Готовит title + summary.

6. Считается TF-IDF score.

7. Статьи сохраняются в SQLite.

8. Публикуется топ-5.

9. В субботу — подборка тулзов.

10. В воскресенье — дайджест недели.

🗺 Дорожная карта

- Расширенный мониторинг (отсутствие новостей, повторяющиеся ошибки).
- Retry + кэширование HTTP-запросов.
- Улучшенная фильтрация (по дате, длине текста, языку).
- Добавление RSS/JSON API.
- LLM-модули для summary/тегов.
- Аналитика качества новостей.

📝 Лицензия
MIT — используй свободно.

⭐ Поддержи проект

Если бот оказался полезным — поставь ⭐ на GitHub. Это помогает проекту расти 🙌
```
---

## `PRODUCTION_DEPLOY.md`

```markdown
# 🛠 PRODUCTION_DEPLOY.md — запуск News Bot в продакшене

Этот документ описывает развёртывание News Bot на реальном сервере (VPS / Dedicated / Railway / Render) с использованием Docker и Docker Compose.

---

## 1. Требования

- Linux-сервер (Ubuntu 20.04+ или аналогичный)
- Docker 24+
- Docker Compose v2+
- Права `sudo`
- Telegram Bot Token
- ID чата/канала для публикации новостей
- ID чата/канала для ошибок (опционально)

---

## 2. Установка Docker и Docker Compose

Пример для Ubuntu:

```bash
sudo apt update
sudo apt install -y ca-certificates curl gnupg lsb-release

curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker.gpg

echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker.gpg] \
  https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" \
  | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

## Архитектура

```mermaid
flowchart LR
  S[APScheduler] --> P[NewsProfessor]
  P --> F["Fetch & Parse"]
  F --> R["Rank (TF-IDF / keywords)"]
  R --> D[(SQLite)]
  D --> T["Telegram Post"]
  D --> M[Monitoring]
  M --> A["Alert to Telegram"]

